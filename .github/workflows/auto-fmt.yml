on:
  pull_request:
    types:
      - opened
      - reopened
      - edited

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-fmt:
    runs-on: ubuntu-latest
    name: auto-fmt
    steps:
      - uses: Bogyie/auto-fmt@v0.0.9
